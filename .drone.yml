---
kind: pipeline
name: default

steps:
  - name: build
    image: golang
    commands:
      - cd exporter
      - go build

---
kind: pipeline
name: release

steps:
  - name: publish
    image: plugins/github-release
    settings:
      api_key:
        from_secret: github_apikey
      files: dist/*
    when:
      event: tag